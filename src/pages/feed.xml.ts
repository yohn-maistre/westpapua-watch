import { SITE } from '../data/site';import { developments } from '../data/developments';
const esc=(s:string)=>s.replace(/[<>&'"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]||c));
export function GET(){const items=developments.map(d=>`<item><title>${esc(d.title.en)}</title><link>${SITE.url}/issues/${d.issueSlug}/</link><guid>${SITE.url}/developments/${d.slug}</guid><pubDate>${new Date(d.updatedAt).toUTCString()}</pubDate><description>${esc(d.summary.en)}</description></item>`).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>${SITE.name}</title><link>${SITE.url}</link><description>${esc(SITE.description.en)}</description>${items}</channel></rss>`;return new Response(xml,{headers:{'content-type':'application/rss+xml; charset=utf-8','cache-control':'public, max-age=1800'}})}
