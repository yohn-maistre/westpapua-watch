import { WorkflowEntrypoint } from 'cloudflare:workers';import { discoverEnabled } from './sources/discover';import { revisitRetryingDevelopments } from './cluster/editorial';import { reconcileRecentDevelopments } from './cluster';import { detectEmergingIssues } from './emerging';import { enqueueLegacyReprocessing } from './ingest/process';
export class NewsCycleWorkflow extends WorkflowEntrypoint<any,unknown>{async run(_event:any,step:any){const legacy=await step.do('reprocess legacy singleton backlog',{retries:{limit:1,delay:'15 seconds'},timeout:'3 minutes'},()=>enqueueLegacyReprocessing(this.env,12));const repair=await step.do('repair retrying developments',{retries:{limit:1,delay:'15 seconds'},timeout:'6 minutes'},()=>revisitRetryingDevelopments(this.env,12));const reconcile=await step.do('reconcile recent developments',{retries:{limit:1,delay:'15 seconds'},timeout:'5 minutes'},()=>reconcileRecentDevelopments(this.env,10));const emerging=await step.do('detect emerging issues',{retries:{limit:1,delay:'20 seconds'},timeout:'5 minutes'},()=>detectEmergingIssues(this.env,18));const items=await step.do('discover enabled publishers',{retries:{limit:2,delay:'20 seconds',backoff:'exponential'},timeout:'3 minutes'},()=>discoverEnabled());const fresh=await step.do('remove known urls',async()=>{const out=[];for(const item of items.slice(0,45)){const row=await this.env.DB.prepare(`SELECT id FROM articles WHERE canonical_url=?`).bind(item.url.replace(/\/$/,'')).first();if(!row)out.push(item)}return out});await step.do('enqueue article ingestion',async()=>{if(!fresh.length)return 0;for(let i=0;i<fresh.length;i+=20)await this.env.INGEST_QUEUE.sendBatch(fresh.slice(i,i+20).map((body:any)=>({body})));return fresh.length});return {legacy,repair,reconcile,emerging,discovered:items.length,enqueued:fresh.length}}}
