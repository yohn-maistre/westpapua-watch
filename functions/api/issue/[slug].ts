type Env={WATCH_ENGINE?:Fetcher};
const headers={'content-type':'application/json; charset=utf-8','cache-control':'public, max-age=60, stale-while-revalidate=180','x-content-type-options':'nosniff'};
export const onRequestGet:PagesFunction<Env>=async({env,params})=>{const slug=String(params.slug||'');if(!/^[a-z0-9-]+$/.test(slug))return new Response(JSON.stringify({error:'Invalid issue'}),{status:400,headers});if(!env.WATCH_ENGINE)return new Response(JSON.stringify({engine:false}),{status:503,headers});try{const response=await env.WATCH_ENGINE.fetch(new Request(`https://watch.internal/issue/${slug}`,{headers:{accept:'application/json'}}));return new Response(response.body,{status:response.status,headers:{...headers,'cache-control':response.ok?'public, max-age=60, stale-while-revalidate=180':'no-store'}})}catch{return new Response(JSON.stringify({engine:false}),{status:503,headers})}}
